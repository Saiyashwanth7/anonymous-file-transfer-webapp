version: "3.8"

services:
  fastapi:
    build: .
    ports: 
      - '8000:8000'
    volumes:
      - ./uploads:/app/uploads
      - ./app.db:/app/app.db
    environment:
      - EMAIL_ADDRESS=${EMAIL_ADDRESS}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}

    env_file:
      - .env